@model SV22T1020292.Models.HR.Employee
@{
    ViewBag.Title = "Xóa nhân viên";
    var photoFile = string.IsNullOrWhiteSpace(Model.Photo) ? "no-photo.png" : Model.Photo;
}
@section PageToolBar {
    <button type="submit" form="formDelete" class="btn btn-danger">
        <i class="bi bi-trash"></i> Xóa
    </button>
    <a href="~/Employee" class="btn btn-secondary ms-1">
        <i class="bi bi-arrow-left"></i> Quay lại
    </a>
}

<div class="card card-danger">
    <div class="card-header">
        <h3 class="card-title mb-0">
            <i class="bi bi-person-x"></i> Xác nhận xóa nhân viên
        </h3>
    </div>

    <div class="card-body">
        <form id="formDelete" asp-action="Delete" asp-route-id="@Model.EmployeeID" method="post">
            @Html.AntiForgeryToken()
            <div class="alert alert-warning mb-3">
                <i class="bi bi-exclamation-triangle"></i>
                Bạn có chắc chắn muốn xóa nhân viên này không?
            </div>

            <div class="row">
                <div class="col-md-2 text-start">
                    <img src="~/images/employees/@photoFile"
                         class="img-thumbnail mb-2"
                         style="width: 100%;"
                         alt=""
                         onerror="this.src='@Url.Content("~/images/employees/no-photo.png")'" />
                </div>

                <div class="col-md-10">
                    <dl class="row mb-0">
                        <dt class="col-sm-3">Họ và tên:</dt>
                        <dd class="col-sm-9">@Model.FullName</dd>

                        <dt class="col-sm-3">Ngày sinh:</dt>
                        <dd class="col-sm-9">@(Model.BirthDate?.ToString("dd/MM/yyyy") ?? "")</dd>

                        <dt class="col-sm-3">Điện thoại:</dt>
                        <dd class="col-sm-9">@Model.Phone</dd>

                        <dt class="col-sm-3">Email:</dt>
                        <dd class="col-sm-9">@Model.Email</dd>

                        <dt class="col-sm-3">Địa chỉ:</dt>
                        <dd class="col-sm-9">@(Model.Address ?? "")</dd>

                        <dt class="col-sm-3">Trạng thái:</dt>
                        <dd class="col-sm-9">
                            @if (Model.IsWorking == true)
                            {
                                <span class="badge bg-success">Đang làm việc</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">Đã nghỉ</span>
                            }
                        </dd>
                    </dl>
                </div>
            </div>
        </form>
    </div>
</div>
